{
  "name": "asset-shop-infra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "synth": "dotenv -e .env -- npx cdk synth",
    "deploy": "dotenv -e .env -- npx cdk deploy --require-approval never",
    "destroy": "dotenv -e .env -- npx cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
